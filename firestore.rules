rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true; // Since the user said "security ko choro mene apne personal use k liye bnaya he", keeping it open for him.
    }
  }
}
